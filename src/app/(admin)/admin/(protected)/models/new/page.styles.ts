/**
 * Tailwind-классы для страницы `/admin/models/new`.
 * Вынесено рядом с page, чтобы заголовок/отступы можно было править без трогания логики.
 */
export const adminNewModelPageClasses = {
  root: 'space-y-6',
  title:
    'font-condensed text-[clamp(32px,4vw,52px)] leading-[0.9] uppercase tracking-[0.14em]'
} as const;
